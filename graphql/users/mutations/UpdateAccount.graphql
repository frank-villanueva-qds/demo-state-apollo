mutation UpdateAccount($input: AccountInput!) {
  accountUpdate(input: $input) {
    user {
      id
      documentNumber
      documentType
    }
    errors {
      field
      code
      message
    }
  }
}
