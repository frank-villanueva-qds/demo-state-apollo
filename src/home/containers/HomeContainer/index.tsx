import ListProducts from '../../components/ListProducts'

const HomeContainer = () => {
  return (
    <main className="w-full bg-gray-600 min-h-screen">
      <ListProducts />
    </main>
  )
}

export default HomeContainer
